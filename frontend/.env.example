# Copy to .env and fill in values
# cp .env.example .env

# API base URL (used when running frontend standalone)
# For Docker/proxy use /api; for local dev use http://localhost:8000/api
VITE_API_URL=http://localhost:8000/api
